export const YEAR: number = 2026;

export const NAME: string = `/mlp/ con ${YEAR}`;
export const DATES: string = `July 10-12, ${YEAR}`;
export const DATES_ISO: string = `${YEAR}-07-10/P3D`;
const test = 0;
